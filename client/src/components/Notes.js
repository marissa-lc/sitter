import React from "react";

function Notes(props) {
  console.log("notes props", props);
  return (
    <div>These are the notes. Notes on the events will be shown.</div>
  );
}

export default Notes;
